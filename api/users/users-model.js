const db = require('../data/db-config');

function findUserBy() {
   
}

function findUserById(user_id) {

}

function addUser({username, password}) {

}

module.exports = {
    findUserBy,
    findUserById,
    addUser
}
